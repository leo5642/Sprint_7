# Sprint_7
Описание Проекта
### **TestPostCourier (test_post_courier) ** 
**Тест**: `test_new_courier`  
**Проверяет **:  
- курьера можно создать;
- запрос возвращает правильный код ответа;
- успешный запрос возвращает {"ok":true}.
- чтобы создать курьера, нужно передать в ручку все обязательные поля.

**Тест**: `test_dabl_courier`  
**Проверяет **:  
- нельзя создать двух одинаковых курьеров;

**Тест**: `test_no_str`  
**Проверяет **:  
- Что если одного из полей нет, запрос возвращает ошибку.

**Тест**: `test_too_login`  
**Проверяет **:  
- Что если создать пользователя с логином, который уже есть, возвращается ошибка.

### **TestPostLogin (test_post_login) ** 
**Тест**: `TestPostLogin`  
**Проверяет **:  
- Что курьер может авторизоваться;
- ЧТо для авторизации нужно передать все обязательные поля.

**Тест**: `test_authorization_no_login_or_password`  
**Проверяет **:  
-Что система вернёт ошибку, если неправильно указать логин или пароль.

**Тест**: `test_authorization_no_valide_login_or_password`  
**Проверяет **:  
-Что если какого-то поля нет, запрос возвращает ошибку.

**Тест**: `test_non_existent`  
**Проверяет **:  
-Что если авторизоваться под несуществующим пользователем, запрос возвращает ошибку.

**Тест**: `test_non_existent`  
**Проверяет **:  
-успешный запрос возвращает id.

### **TestPostOrder (test_post_order) ** 
**Тест**: `test_authorization_color_black`  
**Проверяет **:  
- Что можно указать один из цветов — BLACK или GREY.

**Тест**: `test_authorization_color_crey_and_black`  
**Проверяет **:  
- Что можно указать оба цвета.

**Тест**: `test_authorization_color_no`  
**Проверяет **:  
- Что можно совсем не указывать цвет.

**Тест**: `test_authorization_response`  
**Проверяет **:  
- Что тело ответа содержит track.

### **TestGetOrder (tets_get_track_order) ** 
**Тест**: `test_authorization_color_black`  
Предварительно наличие моего ордера в базе данных 

**Проверяет **:  
- Что в тело ответа возвращается список заказов.


conftest.py содержит фикстуры

requirements Файл с внешними зависимостями 


Папка locators
    -data.py содержит данные для заполнения форм
    -url_and_hand Содержит url и ручки актуальные

allure_results папка содержит Отчет для просмотра команда в терминал (allure serve allure_results)
